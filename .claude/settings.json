{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "timeout": 30,
            "statusMessage": "Auto-saving to git…",
            "command": "$json = $input | Out-String | ConvertFrom-Json; $f = $json.tool_input.file_path; $proj = 'C:/Users/John/Documents/GitHub/Lighting Plot App'; if ($f -like \"*Lighting Plot App*\") { Set-Location $proj; git add -A 2>$null; $st = git status --porcelain 2>$null; if ($st) { git commit -m 'chore: auto-save changes' 2>$null } } 2>$null"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "timeout": 30,
            "statusMessage": "Saving session to git…",
            "command": "$proj = 'C:/Users/John/Documents/GitHub/Lighting Plot App'; if (Test-Path \"$proj/.git\") { Set-Location $proj; git add -A 2>$null; $st = git status --porcelain 2>$null; if ($st) { git commit -m 'chore: session auto-save' 2>$null }; $null } 2>$null"
          }
        ]
      }
    ]
  }
}
